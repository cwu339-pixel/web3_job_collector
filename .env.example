# Global fetch settings
OUTPUT_PATH=web3_jobs.csv            # Global output CSV
MAX_JOBS_PER_SOURCE=200              # Per-source cap
REMOTEOK_TAGS=web3,crypto,blockchain,defi,nft,dao
FILTER_KEYWORDS_WEB3=                # Optional keyword filter (web3/crypto terms)
FILTER_KEYWORDS_ROLE=                # Optional keyword filter (role terms)

# CN/HK pipeline
ENABLE_CN_SOURCES=true               # Set true to fetch CN/HK sources
CN_OUTPUT_PATH=cn_web3_jobs.csv      # CN/HK output CSV
CN_MAX_JOBS_PER_SOURCE=200           # Per-source cap for CN/HK
CAKE_WEB3_LOCATIONS=Hong Kong S.A.R  # Comma-separated locations for Cake Web3

# Matcher IO and batching
INPUT_PATH=web3_jobs.csv             # CSV to score (set to cn_web3_jobs.csv for CN/HK)
OUTPUT_PATH=web3_jobs_scored.csv     # Scored CSV output
MAX_JOBS_TO_SCORE=20                 # Batch size per run (0 = all)
JOBS_OFFSET=0                        # Offset for batching

# OpenAI (do NOT commit real keys)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
